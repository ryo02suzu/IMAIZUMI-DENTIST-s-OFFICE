# スタッフ投稿アプリ（お知らせをテンプレで簡単投稿）

スタッフが `https://imaizumi-dentist-office.com/staff` を開き、合言葉を入れて
テンプレを選び日付などを入力 →「公開」で、サイトのお知らせに反映される仕組みです。

```
/staff ページ（合言葉でロック）
   │  テンプレ選択 + 日付など入力 + プレビュー
   ▼
Cloudflare Functions (/api/news)  ← 合言葉を照合し、書き込み鍵で microCMS に投稿
   ▼
microCMS（お知らせの保管庫）
   ▼
サイトのお知らせ欄に表示（実装済み）
```

- 書き込み用の鍵は**サーバー側に隠す**ので安全（ブラウザには出ません）。
- 表示も投稿も Functions(/api/news) 経由なので、**microCMS のキーは1個（GET+POST）でOK**（フリープラン対応）。
- 投稿ミスは microCMS の管理画面から修正・削除できます。

---

## 必要なセットアップ（管理会社が1回だけ）

### 1. microCMS の APIキーに「GET」と「POST」を許可する
フリープランは APIキー1個までなので、既存の `default` キーに両方の権限を付けます。
1. microCMS 管理画面 → `サービス設定 > APIキー` → `default` を開く
2. お知らせ(`news`)に対して **`GET`（取得）と `POST`（登録）の両方を ON**
3. 保存し、キーの文字列を控える

### 2. Cloudflare Pages に環境変数を設定
`Workers & Pages > imaizumi-dentist-s-office > Settings > Variables and Secrets`
（すべて Functions がサーバー側で使います。`VITE_` は付けません＝ブラウザに出ません）

| 変数名 | 値 | 種別 |
|---|---|---|
| `MICROCMS_SERVICE_DOMAIN` | `t1ay8zpm8u` | Plaintext |
| `MICROCMS_API_KEY` | GET+POST 権限のAPIキー（手順1） | Secret |
| `STAFF_PASSWORD` | スタッフ用の合言葉（自由に決める） | Secret |
| `MICROCMS_NEWS_ENDPOINT` | `news`（任意・省略可） | Plaintext |

> これらは Functions が**実行時に読みます**。再デプロイ後に有効になります。

### 3. 再デプロイ
`Deployments > 最新のデプロイ > Retry deployment`、または `main` に何かpushすると自動で再ビルドされます。

---

## スタッフ向けの使い方（現場マニュアル）

1. `https://imaizumi-dentist-office.com/staff` を開く（ブックマーク推奨）
2. **合言葉**を入力
3. **種類を選ぶ**（終日休診 / 午後休診 / 臨時休診 / 自由入力）
4. **日付**などを入力すると、下に**プレビュー**（実際に載る文章）が出る
5. 内容を確認して **「この内容で公開する」** → サイトに反映

### 例：「6/5を休みにしたい」
- 種類：**終日休診** を選ぶ
- 休診日：`6/5` を選ぶ
- → 「6/5(金) 休診のお知らせ」という文章が自動で作られる（曜日も自動）
- 「この内容で公開する」を押すだけ

### 困ったとき
- 「パスワードが違います」→ 合言葉を確認（大文字小文字も区別されます）
- 投稿を間違えた → microCMS の管理画面から修正・削除
- 反映されない → 画面を再読み込み。それでもダメなら管理会社へ
