# Replit からの移行ガイド（Cloudflare Pages + microCMS）

このサイトを **Replit に依存せず自分たちで運用** できるようにするための手順書です。

- **ホスティング（公開）**: Cloudflare Pages（無料）
- **お知らせ更新**: microCMS（無料枠／日本語の管理画面）
- **ドメイン**: 既存の `imaizumi-dentist-office.com`（ムームードメイン）をそのまま使用

移行後の月額コストは **基本的にドメイン代だけ**。Replit は解約できます。

> 役割分担の前提
> - **管理会社（あなた）**: 下記「初期セットアップ」を1回だけ行う
> - **歯科医院の現場スタッフ**: 日々の「お知らせ更新」だけを担当（→ 末尾の[スタッフ向けマニュアル](#スタッフ向けマニュアルお知らせの更新)）

---

## 仕組みの全体像

```
   GitHub（ソースコード）──┐
                          ├─► Cloudflare Pages（自動ビルド＆公開）─► imaizumi-dentist-office.com
   microCMS（お知らせ）────┘
        ▲
        └ 現場スタッフが管理画面からお知らせを入力・公開
```

- サイトのコードは GitHub にあり、`main` ブランチに push されると Cloudflare Pages が**自動でビルド・公開**します。
- お知らせは microCMS に保存され、サイトは表示時に microCMS から取得します。
  **スタッフが microCMS で公開すれば、サイトはすぐに反映されます（再ビルド不要）。**
- microCMS をまだ設定していない間は、`src/data/newsData.ts` のお知らせがそのまま表示されます（今までと同じ）。**段階的に移行できる**ので安心です。

---

## 初期セットアップ（管理会社が1回だけ実施）

### ステップ1. GitHub リポジトリを自分の管理下に

すでに `ryo02suzu/imaizumi-dentist-s-office` がある前提です。
このリポジトリの所有・管理権限があることを確認してください（Cloudflare から接続します）。

### ステップ2. microCMS をセットアップ

1. https://microcms.io/ で無料登録し、**サービス**を作成
   （このとき決めた `XXXX.microcms.io` の **XXXX がサービスID**）。
2. **API を作成**します。
   - API名: `お知らせ`（任意）
   - **エンドポイント: `news`**（※コードの既定値。変える場合は環境変数 `VITE_MICROCMS_NEWS_ENDPOINT` で指定）
   - API の型: **リスト形式**
3. API スキーマ（入力項目）を次の通り作成します。**フィールドID は下表のとおり正確に**設定してください（コードと対応しています）。

   | フィールドID | 表示名 | 種類 | 必須 | 備考 |
   |---|---|---|---|---|
   | `title` | タイトル | テキストフィールド | ✓ | 例：「4/23(木) 休診のお知らせ」 |
   | `date` | 日付 | 日時 | ✓ | お知らせの日付 |
   | `category` | カテゴリ | セレクト | | 選択肢に `診療案内` `休診案内` `お知らせ` を登録 |
   | `excerpt` | 抜粋 | テキストエリア | | 一覧に出る要約。空なら本文から自動生成 |
   | `body` | 本文 | テキストエリア | ✓ | 改行はそのまま表示されます |

   > 本文 `body` は「テキストエリア」を推奨します（現状の表示が改行をそのまま反映する作りのため）。リッチエディタにすると表示が崩れる可能性があります。

4. **APIキーを取得**します（`サービス設定 > APIキー`）。
   - **GET だけが有効なキー**を作成してください（ブラウザに埋め込まれるため、書き込み・管理権限は付けない）。
5. 既存のお知らせ（`src/data/newsData.ts` の6件）を、microCMS の管理画面から登録します。
   今は**6月時点で4月以前の休診案内が残っている**ので、この機会に古いものは登録せず、最新情報に整理することをおすすめします。

### ステップ3. Cloudflare Pages で公開

1. https://dash.cloudflare.com/ で無料登録 → **Workers & Pages > 作成 > Pages > Git に接続**。
2. 上記の GitHub リポジトリを選択。
3. **ビルド設定**を次のように入力：

   | 項目 | 値 |
   |---|---|
   | プロダクションブランチ | `main` |
   | フレームワークプリセット | なし（None） |
   | ビルドコマンド | `pnpm install --frozen-lockfile && pnpm --filter @workspace/imaizumi-dental run build` |
   | ビルド出力ディレクトリ | `artifacts/imaizumi-dental/dist/public` |
   | ルートディレクトリ | （空欄＝リポジトリ直下） |

4. **環境変数**を設定（`設定 > 環境変数`、Production と Preview の両方）：

   | 変数名 | 値 |
   |---|---|
   | `NODE_VERSION` | `22` |
   | `BASE_PATH` | `/` |
   | `VITE_MICROCMS_SERVICE_DOMAIN` | ステップ2の **サービスID**（`XXXX`） |
   | `VITE_MICROCMS_API_KEY` | ステップ2の **GET専用APIキー** |

5. 「保存してデプロイ」。`*.pages.dev` の仮URLで表示を確認します。

> microCMS をまだ設定していない場合は、`VITE_MICROCMS_*` を空のままでもOK。
> その場合は `newsData.ts` の内容が表示されます（移行作業を分けて進められます）。

### ステップ4. 独自ドメインを接続（ムームードメイン）

1. Cloudflare Pages の `カスタムドメイン > ドメインを設定` で
   `imaizumi-dentist-office.com`（と必要なら `www`）を追加。
2. 画面に表示される **CNAME（または案内されるレコード）** を、
   **ムームードメインの DNS 設定（ムームーDNS）** に登録します。
   （現在 Replit 向けに設定しているレコードを、Cloudflare 向けに**置き換え**ます）
3. DNS が反映されると、独自ドメインで新しいサイトが表示されます（反映に数分〜数時間）。

### ステップ5. Replit を解約

独自ドメインで Cloudflare 版が正しく表示され、お知らせの追加・表示まで確認できたら、
**Replit のデプロイ／プランを解約**して問題ありません。

---

## 任意：お知らせ公開を「即時反映」させたい場合

サイトは表示時に microCMS から取得するため、基本は**スタッフの公開だけで反映**されます（追加設定不要）。
ブラウザのキャッシュ等でさらに確実に最新化したい場合は、microCMS の **Webhook** から
Cloudflare Pages の **デプロイフック（Deploy Hook）URL** を呼ぶように設定すると、
公開のたびに再ビルドされます（必須ではありません）。

---

## ローカルで動作確認したいとき（管理会社向け）

```bash
pnpm install
# お知らせを microCMS から取りたい場合は .env.local を作成（.env.example をコピー）
cp artifacts/imaizumi-dental/.env.example artifacts/imaizumi-dental/.env.local
# 値を記入してから:
pnpm --filter @workspace/imaizumi-dental run dev
```

`.env.local` は `.gitignore` 済みなので、**APIキーが GitHub に上がることはありません**。

---

## スタッフ向けマニュアル（お知らせの更新）

> 難しい操作は不要です。microCMS の管理画面だけで完結します。

### お知らせを追加する
1. https://app.microcms.io/ にログイン。
2. 左メニューの「**お知らせ**」を開く →「**追加**」。
3. 次を入力：
   - **タイトル**：例「5/3(土) 午後休診のお知らせ」
   - **日付**：お知らせの日付
   - **カテゴリ**：`休診案内` / `診療案内` / `お知らせ` から選ぶ
   - **抜粋**：一覧に出る短い説明（空欄でもOK）
   - **本文**：詳しい内容（改行はそのまま表示されます）
4. 右上の「**公開**」を押す → サイトに反映されます。

### お知らせを直す・消す
- 一覧から該当のお知らせを開いて編集し「公開」、または「削除」。

### 困ったとき
- 反映されない場合は、ブラウザを再読み込み（更新）してみてください。
- それでも直らない場合は管理会社にご連絡ください。
